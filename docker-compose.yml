services:
  bot:
    build: .
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./token.json:/app/token.json
